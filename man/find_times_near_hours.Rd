% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{find_times_near_hours}
\alias{find_times_near_hours}
\title{Find Times Near Specific Hours}
\usage{
find_times_near_hours(
  data,
  hours = c(6, 12, 18),
  max_days = 7,
  tolerance_minutes = 30
)
}
\arguments{
\item{data}{Data frame with time column}

\item{hours}{Numeric vector of hours (0-23). Default is c(6, 12, 18) for morning, noon, evening}

\item{max_days}{Integer. Maximum number of days to search. Default is 7}

\item{tolerance_minutes}{Numeric. How close to target hour (in minutes). Default is 30}
}
\value{
POSIXct vector of matching times
}
\description{
Helper function to find measurement times close to specific hours of the day.
Useful for selecting consistent time points across multiple days.
}
\examples{
\dontrun{
data <- process_sensor_data("teros54_data.xlsx")

# Find noon measurements
noon_times <- find_times_near_hours(data, hours = 12)
plot_soil_profile(data, time_points = noon_times)

# Find morning, noon, evening measurements
daily_times <- find_times_near_hours(data, hours = c(6, 12, 18))
plot_soil_profile(data, time_points = daily_times)
}

}
